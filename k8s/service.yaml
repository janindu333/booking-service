apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: saloon-system
  labels:
    app: booking-service
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
